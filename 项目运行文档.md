# 乐嘉项目运行文档

> 适用范围：本地开发、测试环境联调。本文基于当前工作区源码和配置整理，未覆盖生产发布流程。

## 1. 项目组成

| 工程 | 目录 | 作用 | 技术/入口 |
| --- | --- | --- | --- |
| 注册服务 | `注册服务/tfcloud-eureka-server` | Eureka 服务注册中心 | Spring Boot，默认端口 20000 |
| 账号服务 | `账号服务/tfcloud-account/tfcloud-account-server` | 用户、角色、菜单、权限、认证 | Spring Boot，当前已编译配置端口 21001 |
| 文件服务 | `文件服务/tfcloud-file-parent/tfcloud-file-server` | 文件上传、下载、引用和访问日志 | Spring Boot，当前已编译配置端口 21003 |
| 业务服务 | `业务服务/lejia-server` | 商城、订单、商户、骑手、支付、营销等核心业务 | Spring Boot，端口 23505 |
| PC 管理端 | `pc前端-后台管理端/lejia-web` | 运营后台 | Vue 2 + Element UI |
| 商城小程序 | `微信小程序-商城端/lejia-smallprogram` | 消费者端 | 原生微信小程序 |
| 骑手小程序 | `微信小程序-骑手端/lejia-rider` | 骑手接单和配送 | 原生微信小程序 |
| 商户小程序 | `微信小程序-商户端/lejia-merchant` | 商户订单、商品、钱包和核销 | 原生微信小程序 |

后端服务不是完全独立的微服务集合，而是以 `lejia-server` 为核心的模块化业务服务，同时依赖账号、文件和注册中心。

## 2. 当前机器检查结果

本次检查到：

- Java：`17.0.20`
- Maven：未安装或不在 PATH
- Node.js：`v24.12.0`
- npm：`11.6.2`

项目 Maven 配置目标为 Java 8；PC 前端是较老的 Vue CLI 4 + Vue 2 项目。因此建议使用以下工具链：

| 工具 | 建议版本 | 说明 |
| --- | --- | --- |
| JDK | 8 | Maven 配置中的 `source/target` 为 1.8 |
| Maven | 3.5.3 以上，建议 3.8.x | 工程内部分服务带 Maven Wrapper |
| Node.js | 14 或 16 LTS | 更适合 Vue CLI 4 和旧版依赖 |
| npm | 随 Node.js 安装 | 优先使用 `npm ci` |
| 微信开发者工具 | 最新稳定版 | 用于三个小程序编译和调试 |
| MySQL、Redis、RabbitMQ、Elasticsearch | 与目标环境一致 | 后端配置依赖这些基础设施 |

另外，Maven 依赖中存在大量 `cn.tofocus` 私有组件。必须具备公司 Maven 仓库访问权限，或准备好对应的 `settings.xml` 和本地 Maven 仓库。

## 3. 启动前必须准备的基础设施

后端启动前至少需要确认以下服务可访问：

1. MySQL：业务库、账号库、文件库。
2. Redis：账号、业务缓存和配置缓存。
3. RabbitMQ：异步消息和文件服务相关消息。
4. Elasticsearch：文件访问日志及部分检索能力。
5. Eureka：服务注册发现。
6. 微信、支付、物流、京东等第三方接口：仅在对应功能联调时需要。

不要直接把生产数据库用于本地开发。建议先复制一套测试数据库，并单独配置数据库账号、Redis 前缀和文件存储目录。

## 4. 数据库初始化

### 4.1 业务库

业务迁移文件位于：

```text
业务服务/lejia-server/lejia-server/src/main/resources/db/migration/
```

当前包含从 `V1` 到后续多个版本的 Flyway 迁移脚本。`业务服务/lejia-server/bin/db/migration/` 下也有一份发行目录中的迁移文件，开发时以源码目录为准。

### 4.2 账号库

账号迁移文件位于：

```text
账号服务/tfcloud-account/tfcloud-account-server/src/main/resources/db/migration/
```

账号服务另外提供了默认角色和权限处理脚本：

```text
账号服务/tfcloud-account/tfcloud-account-server/重置默认角色和权限.sql
```

### 4.3 文件库

文件服务迁移文件位于：

```text
文件服务/tfcloud-file-parent/tfcloud-file-server/src/main/resources/db/migration/
```

### 4.4 数据库执行原则

- 优先让应用内置的 Flyway 自动执行迁移。
- 不要反复手工执行已经执行过的 `V` 脚本。
- 如果出现 Flyway checksum 错误，不要直接删除迁移历史表，应先确认数据库和源码版本是否匹配。
- 业务服务、账号服务、文件服务使用不同数据库时，要分别检查配置。

## 5. 环境配置说明

### 5.1 配置加载顺序

后端配置通常按以下顺序加载：

```text
application-common.properties
→ application-{环境}.properties
→ application-running.properties
```

后面的配置会覆盖前面的配置。

主要配置文件：

- 业务服务：`业务服务/lejia-server/lejia-server/src/main/resources/`
- 账号服务：`账号服务/tfcloud-account/tfcloud-account-server/src/main/resources/`
- 文件服务：`文件服务/tfcloud-file-parent/tfcloud-file-server/src/main/resources/`
- Eureka：`注册服务/tfcloud-eureka-server/src/main/resources/`

当前源码默认 profile 并不完全一致：

| 服务 | 当前默认 profile |
| --- | --- |
| Eureka | `common,local,running` |
| 业务服务 | `common,release,running` |
| 账号服务 | `common,preProduct,running` |
| 文件服务 | `common,preProduct,running` |

因此，不能认为直接启动后就是“全本地环境”。最快的方式是连接项目现有测试环境；如果要真正全本地运行，需要为账号服务和文件服务补充本地 profile，并把所有依赖地址改为本机或局域网地址。

### 5.2 Maven 资源过滤

账号服务和文件服务的 `application.properties` 中存在类似 `@tofocus.account.port@`、`@tofocus.file.port@` 的占位符。这些值需要在 Maven 构建时进行资源过滤，不能直接把源码目录当作最终运行目录。

如果启动时出现以下问题，通常说明资源没有正确过滤：

- `server.port=@...@`
- 数据库、Redis 或 Eureka 地址仍然是 `@...@`
- Spring 启动时报端口格式错误

构建完成后，应检查 `target/classes/application.properties` 是否已经生成真实的服务名、端口和环境配置。

### 5.3 本地配置建议

建议先复制对应环境配置，再建立本地专用配置，至少确认：

- Eureka 地址
- MySQL URL、用户名和密码
- Redis 地址、端口、密码和 prefix
- RabbitMQ 地址、用户名、密码和 virtual host
- Elasticsearch 地址
- 账号服务地址
- 文件服务公网地址和内网地址
- 微信小程序 AppID、支付配置和回调地址

不要把新的本地密码、支付密钥和生产凭据提交到 Git。

## 6. 后端构建

### 6.1 业务服务

在 PowerShell 中执行：

```powershell
cd "E:\Projects\0源代码202607 (2)\业务服务\lejia-server\lejia-server"
.\mvnw.cmd -f ..\pom.xml -DskipTests package
```

如果已全局安装 Maven，也可以在 `业务服务/lejia-server` 根目录执行：

```powershell
mvn -DskipTests package
```

业务服务的启动类是：

```text
cn.tofocus.lejia.ProjectApplication
```

### 6.2 账号服务

账号工程缺少工作区级别的父 POM，父依赖需要从私有 Maven 仓库解析。构建前应先确认公司 Maven 仓库和 `settings.xml` 已配置。

```powershell
cd "E:\Projects\0源代码202607 (2)\账号服务\tfcloud-account\tfcloud-account-server"
.\mvnw.cmd -f ..\pom.xml -DskipTests package
```

账号服务启动类是：

```text
cn.tofocus.account.AccountApplication
```

### 6.3 文件服务

```powershell
cd "E:\Projects\0源代码202607 (2)\文件服务\tfcloud-file-parent\tfcloud-file-server"
.\mvnw.cmd -f ..\pom.xml -DskipTests package
```

文件服务启动类是：

```text
cn.tofocus.file.Application
```

### 6.4 Eureka

```powershell
cd "E:\Projects\0源代码202607 (2)\注册服务\tfcloud-eureka-server"
.\mvnw.cmd -DskipTests package
```

Eureka 启动类是：

```text
cn.tofocus.eureka.EurekaApplication
```

## 7. 后端启动顺序

推荐顺序如下：

```text
MySQL / Redis / RabbitMQ / Elasticsearch
        ↓
Eureka
        ↓
账号服务、文件服务
        ↓
业务服务
        ↓
PC 管理端和微信小程序
```

### 7.1 直接运行打包后的 JAR

构建成功后，在相应服务的 `target` 目录找到可执行 JAR，使用：

```powershell
java -jar <服务目录>\target\<可执行jar>.jar
```

也可以通过命令行覆盖 profile：

```powershell
java -jar <可执行jar>.jar --spring.profiles.active=common,local,running
```

注意：只有配置文件和 Maven 资源过滤都支持该 profile 时，才能这样启动。账号服务和文件服务目前没有完整的本地 profile，不能只靠命令行强行切换。

### 7.2 当前已编译配置中的端口

| 服务 | 端口 | 健康检查建议 |
| --- | ---: | --- |
| Eureka | 20000 | `http://127.0.0.1:20000/` |
| 账号服务 | 21001 | `http://127.0.0.1:21001/actuator/info` |
| 文件服务 | 21003 | `http://127.0.0.1:21003/actuator/info` |
| 业务服务 | 23505 | `http://127.0.0.1:23505/actuator/info` |

这些端口来自当前工作区已经生成的 `target/classes` 配置和 Maven 属性；如果重新选择 profile 或使用外部配置，实际端口可能变化。

### 7.3 Linux/MINGW 启动脚本

业务服务提供：

```text
业务服务/lejia-server/shell/start.sh
业务服务/lejia-server/shell/stop.sh
```

脚本假定当前目录中已经有 JAR，并且存在 `config/application.properties`。它更适合发行包目录或 Linux/MINGW 环境，不建议直接在 Windows PowerShell 中使用。Windows 本地开发优先使用 IDE、`mvnw.cmd` 或 `java -jar`。

## 8. PC 管理端运行

```powershell
cd "E:\Projects\0源代码202607 (2)\pc前端-后台管理端\lejia-web"
npm ci
npm run serve
```

默认开发地址通常是：

```text
http://localhost:8080
```

相关脚本：

```powershell
npm run serve       # 开发服务
npm run build       # 生产构建
npm run lint        # ESLint 检查
npm run test        # 以 test 模式进行构建
```

开发代理配置位于：

```text
pc前端-后台管理端/lejia-web/vue.config.js
```

当前 `/api` 和 `/foo` 代理指向固定测试服务器。如果后端运行在本机，需要将代理目标改成实际业务服务地址，或者按照 `src/apiConfig`、`src/plugins/axios` 的配置调整接口地址。

## 9. 微信小程序运行

三个小程序都应使用微信开发者工具分别导入自己的工程根目录：

| 小程序 | 导入目录 |
| --- | --- |
| 商城端 | `微信小程序-商城端/lejia-smallprogram` |
| 骑手端 | `微信小程序-骑手端/lejia-rider` |
| 商户端 | `微信小程序-商户端/lejia-merchant` |

进入各目录安装依赖：

```powershell
cd "E:\Projects\0源代码202607 (2)\微信小程序-商城端\lejia-smallprogram"
npm ci

cd "E:\Projects\0源代码202607 (2)\微信小程序-骑手端\lejia-rider"
npm ci

cd "E:\Projects\0源代码202607 (2)\微信小程序-商户端\lejia-merchant"
npm ci
```

导入后，在微信开发者工具中：

1. 确认 AppID 与 `project.config.json` 一致。
2. 商城端执行“构建 npm”，因为它启用了 npm 构建配置。
3. 开发阶段可开启“不校验合法域名、web-view 业务域名、TLS 版本以及 HTTPS 证书”。
4. 真机调试时必须使用手机可访问的局域网地址或已备案 HTTPS 域名，不能依赖 `127.0.0.1`。
5. 检查各端 `app.js` 中的 `ajax_url`、`ascription` 和文件服务地址是否指向当前环境。

三个小程序的请求公共头在各自的 `utils/http.js` 中组装，通常包含 `openid`、`ascription`、来源和市场信息。接口能够访问但返回未登录、无市场或无商品时，优先检查这些参数。

## 10. 最小验证流程

后端全部启动后，建议按以下顺序验证：

1. 打开 Eureka 页面，确认业务、账号、文件服务已注册。
2. 访问业务服务的 `/actuator/info`。
3. 打开 PC 管理端，验证登录、验证码和菜单加载。
4. 打开商城小程序，验证微信登录、首页配置、分类和商品列表。
5. 打开商户端，验证商户登录、订单列表和商品管理。
6. 打开骑手端，验证骑手登录、订单列表和完成订单。
7. 最后再测试下单、支付、退款、提现、物流等涉及第三方或异步消息的功能。

## 11. 常见问题

| 现象 | 可能原因 | 处理方式 |
| --- | --- | --- |
| Maven 找不到 `cn.tofocus` 依赖 | 未配置私有 Maven 仓库或未连接公司网络 | 检查 Maven `settings.xml`、私服地址和 VPN/内网连接 |
| 启动时报 `@xxx@` 端口或地址错误 | Maven 资源过滤未生效 | 从 Maven 构建，不要直接运行源码资源；检查 `target/classes` |
| Spring 启动连接数据库失败 | profile 指向了不可访问的环境或数据库未初始化 | 检查当前 profile、数据库连接和迁移状态 |
| 业务服务注册不到 Eureka | Eureka 地址、服务名或端口不一致 | 检查各环境配置中的 `eureka.client.serviceUrl.defaultZone` |
| 返回 401/403 | OAuth2 认证、账号服务或请求头不完整 | 先确认账号服务，再检查 Token、openid、ascription |
| 文件上传失败 | 文件服务未启动或文件公网/内网地址不一致 | 检查文件服务、文件 URL 和业务服务文件配置 |
| PC 页面接口跨域或 404 | Vue 代理仍指向旧测试地址 | 修改 `vue.config.js` 的 `/api`、`/foo` 代理 |
| 小程序首页空白或无商品 | `ajax_url`、`ascription`、openid 或市场配置不匹配 | 检查小程序 `app.js` 和请求公共头 |
| Node 依赖安装或编译失败 | Node 24 对旧版 Vue 工程兼容性不好 | 切换 Node 14/16 LTS，再执行 `npm ci` |
| Windows 无法执行 `start.sh` | 这是 Bash 脚本 | 使用 `mvnw.cmd`、IDE 或 `java -jar` |
| 端口被占用 | 已有旧服务或其他程序占用 | 使用 `netstat -ano` 检查并停止对应进程，或修改端口 |

## 12. 当前项目的启动阻塞点

在当前工作区直接“全本地一键启动”前，还需要补齐以下内容：

1. 安装并选择 JDK 8；当前机器只有 JDK 17。
2. 安装 Maven 或确认 Maven Wrapper 可以访问网络。
3. 配置公司私有 Maven 仓库及 `cn.tofocus` 依赖。
4. 明确本地 MySQL、Redis、RabbitMQ、Elasticsearch 的地址和账号。
5. 为账号服务和文件服务补齐本地 profile 或统一测试环境配置。
6. 确定各端使用的 `ajax_url`、文件服务地址和 `ascription`。
7. 确认测试数据库已经执行正确版本的 Flyway 迁移。
8. 清理或忽略 `target`、`.DS_Store` 等构建和系统产物，避免误提交。

本文件只新增运行说明，没有修改现有源码、配置或依赖。
