# 后端基础设施部署操作手册

> 适用项目：乐嘉 / 泽起邻里商城后端
>
> 适用环境：本地开发、测试、预生产。生产部署需额外补充高可用、备份、监控、密钥和变更审批流程。
>
> 本手册只写部署步骤和配置要求，不包含当前工程中的真实密码、支付密钥或 Token。

## 1. 部署目标

后端运行依赖以下基础设施：

```text
MySQL ───────────────┐
Redis ───────────────┤
RabbitMQ ────────────┼── 业务服务、账号服务、文件服务
Elasticsearch ───────┤
Eureka 注册中心 ─────┘
```

业务服务、账号服务和文件服务是 Spring Boot 应用；Eureka 是服务注册中心。MySQL、Redis、RabbitMQ、Elasticsearch 不是当前源码自带的容器服务，仓库也没有 `docker-compose.yml` 或统一安装脚本，需要单独准备。

## 2. 服务和端口规划

### 2.1 应用服务端口

当前项目已编译配置中使用的端口如下：

| 服务 | 默认端口 | 作用 |
| --- | ---: | --- |
| Eureka | 20000 | 服务注册和发现 |
| 账号服务 | 21001 | 账号、权限、认证 |
| 文件服务 | 21003 | 文件上传、下载、引用 |
| 业务服务 | 23505 | 商城和配送核心业务 |

### 2.2 基础设施端口

以下是常见默认端口，最终以实际安装配置为准：

| 组件 | 默认端口 | 访问范围 |
| --- | ---: | --- |
| MySQL | 3306 | 仅应用服务器和运维网络 |
| Redis | 6379 | 仅应用服务器 |
| RabbitMQ AMQP | 5672 | 仅应用服务器 |
| RabbitMQ 管理台 | 15672 | 仅运维网络 |
| Elasticsearch HTTP | 9200 | 仅文件服务、业务服务和运维网络 |
| Elasticsearch 节点通信 | 9300 | 仅 ES 集群节点；单机可不开放 |

不要将 Redis、RabbitMQ、Elasticsearch 直接暴露到公网。生产环境应使用内网 IP、VPC、安全组和主机防火墙限制访问。

## 3. 当前项目的资源要求

### 3.1 MySQL

项目使用 MySQL 驱动、JPA 和 Flyway，公共配置包含：

- JDBC 驱动：`com.mysql.cj.jdbc.Driver`
- Hibernate 方言：MySQL 5 InnoDB
- `spring.jpa.hibernate.ddl-auto=validate`
- `spring.flyway.enabled=true`
- 业务服务连接池最大连接数：50
- 账号服务和文件服务连接池最大连接数：50

建议使用支持当前 JDBC 驱动的 MySQL 5.7 或 8.x，并统一使用 `utf8mb4`、`Asia/Shanghai` 时区。

### 3.2 Redis

项目使用 Redis 保存缓存、配置、登录相关状态和部分业务临时数据。不同环境通过 prefix 隔离：

| 环境 | 常见 prefix |
| --- | --- |
| 测试 | `test` |
| 预生产/生产 | `product` 或项目指定 prefix |

不同环境不要共用相同 Redis prefix。若共用 Redis 实例，必须使用不同数据库、账号权限或不同业务前缀。

### 3.3 RabbitMQ

项目使用 RabbitMQ 进行异步消息和部分服务间通信，公共配置使用 virtual host `/`，并启用消息发布确认相关配置。

建议每个环境使用独立 virtual host，例如：

```text
/lejia-test
/lejia-pre
/lejia-prod
```

如果现有代码或配置固定使用 `/`，则至少使用独立 RabbitMQ 用户和独立交换机、队列前缀，避免测试消息进入生产环境。

### 3.4 Elasticsearch

文件服务配置了 `spring.elasticsearch.uris`，业务配置中同时出现 Elasticsearch HTTP 地址和节点通信地址：

- HTTP 查询和管理通常使用 `9200`
- 节点通信配置中可能使用 `9300`

文件服务会记录文件访问日志，部署文件服务时必须确认 Elasticsearch 地址可访问。单机开发环境只需要 Elasticsearch HTTP 接口，不要为了兼容旧配置而在公网开放 9300。

## 4. 部署前检查

### 4.1 主机和网络

准备服务器并记录以下信息：

```text
应用主机 IP：<APP_HOST>
MySQL 地址：<MYSQL_HOST>:3306
Redis 地址：<REDIS_HOST>:6379
RabbitMQ 地址：<MQ_HOST>:5672
Elasticsearch 地址：<ES_HOST>:9200
Eureka 地址：http://<EUREKA_HOST>:20000/eureka/
```

从应用主机检查端口：

```bash
nc -vz <MYSQL_HOST> 3306
nc -vz <REDIS_HOST> 6379
nc -vz <MQ_HOST> 5672
nc -vz <ES_HOST> 9200
nc -vz <EUREKA_HOST> 20000
```

Windows PowerShell 可以使用：

```powershell
Test-NetConnection <MYSQL_HOST> -Port 3306
Test-NetConnection <REDIS_HOST> -Port 6379
Test-NetConnection <MQ_HOST> -Port 5672
Test-NetConnection <ES_HOST> -Port 9200
Test-NetConnection <EUREKA_HOST> -Port 20000
```

### 4.2 账户和权限

至少准备以下独立账号：

| 账号 | 用途 |
| --- | --- |
| 业务数据库账号 | 访问商城业务库和执行 Flyway |
| 账号数据库账号 | 访问用户权限库和执行 Flyway |
| 文件数据库账号 | 访问文件记录库和执行 Flyway |
| Redis 账号 | 业务缓存访问 |
| RabbitMQ 应用账号 | 业务消息访问 |
| Elasticsearch 账号 | 文件访问日志读写 |
| 运维账号 | 管理台、备份和故障处理 |

应用账号不应使用 MySQL root、Redis 默认账号或 RabbitMQ 管理员账号。

## 5. MySQL 部署

### 5.1 安装和基础配置

使用操作系统包管理器或公司标准镜像安装 MySQL。安装后确认：

- MySQL 监听地址允许应用主机访问。
- 字符集为 `utf8mb4`，时区为 `Asia/Shanghai`。
- 数据目录、日志目录和备份目录位于持久化磁盘。
- 已启用慢查询日志和错误日志。
- 已设置合理的连接数和 InnoDB buffer pool。

### 5.2 创建数据库

当前工程配置中出现的数据库名如下，实际使用哪个名称取决于环境 profile：

| 服务 | 测试环境常见库名 | 预生产/生产常见库名 |
| --- | --- | --- |
| 业务服务 | `zyysc4` | `zyysc_small` 或 `zyysc` |
| 账号服务 | `tofocus_account` | `tf_product_account` |
| 文件服务 | `tofocus_file` | `tf_product_file` |

创建数据库示例：

```sql
CREATE DATABASE `zyysc4`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

CREATE DATABASE `tofocus_account`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

CREATE DATABASE `tofocus_file`
  DEFAULT CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

正式部署时请替换为实际环境数据库名，不要直接复制测试库名。

### 5.3 创建应用账号

示例仅展示权限范围，密码必须通过密码管理系统注入：

```sql
CREATE USER '<BUSINESS_DB_USER>'@'<APP_HOST>' IDENTIFIED BY '<STRONG_PASSWORD>';
CREATE USER '<ACCOUNT_DB_USER>'@'<APP_HOST>' IDENTIFIED BY '<STRONG_PASSWORD>';
CREATE USER '<FILE_DB_USER>'@'<APP_HOST>' IDENTIFIED BY '<STRONG_PASSWORD>';

GRANT ALL PRIVILEGES ON `zyysc4`.* TO '<BUSINESS_DB_USER>'@'<APP_HOST>';
GRANT ALL PRIVILEGES ON `tofocus_account`.* TO '<ACCOUNT_DB_USER>'@'<APP_HOST>';
GRANT ALL PRIVILEGES ON `tofocus_file`.* TO '<FILE_DB_USER>'@'<APP_HOST>';

FLUSH PRIVILEGES;
```

生产环境如果要求更小权限，应根据 Flyway 执行阶段和运行阶段拆分账号，而不是长期给应用账号授予所有权限。

### 5.4 数据库迁移

迁移脚本位置：

```text
业务服务/lejia-server/lejia-server/src/main/resources/db/migration/
账号服务/tfcloud-account/tfcloud-account-server/src/main/resources/db/migration/
文件服务/tfcloud-file-parent/tfcloud-file-server/src/main/resources/db/migration/
```

首次部署建议：

1. 确认数据库是新库或已经完成备份。
2. 启动账号服务，让账号库 Flyway 完成初始化。
3. 启动文件服务，让文件库 Flyway 完成初始化。
4. 启动业务服务，让业务库 Flyway 完成初始化。
5. 检查三套数据库中的 Flyway 历史表和最后一个版本号。
6. 按需要执行默认角色、菜单和权限初始化脚本。

不要把 `ddl-auto` 改成 `update` 作为正式部署方案。项目当前使用 `validate`，数据库结构应由迁移脚本管理。

## 6. Redis 部署

### 6.1 安装和配置

安装 Redis 后至少完成以下配置：

- 绑定内网地址或应用主机地址。
- 设置访问密码或 ACL 用户。
- 禁止公网访问。
- 设置持久化策略：开发环境可使用 RDB，生产环境按数据重要性选择 RDB + AOF。
- 设置最大内存和淘汰策略。
- 配置慢查询日志和自动重启。

示例检查：

```bash
redis-cli -h <REDIS_HOST> -p 6379 -a '<REDIS_PASSWORD>' PING
```

生产环境不要把密码直接写进 shell 历史记录，建议使用交互式输入、环境变量或密码文件。

### 6.2 环境隔离

业务服务、账号服务、文件服务的 Redis 配置必须保持一致的环境标识。推荐：

```text
测试：Redis 独立实例或 prefix=test
预生产：Redis 独立实例或 prefix=pre
生产：Redis 独立实例或 prefix=product
```

切换环境后必须清理或隔离旧缓存，尤其是登录状态、购物车、订单状态和系统配置缓存。

## 7. RabbitMQ 部署

### 7.1 安装和配置

安装 RabbitMQ 和 Erlang 兼容运行时后：

1. 创建应用用户。
2. 创建对应环境的 virtual host。
3. 为应用用户授予该 virtual host 的配置、写入和读取权限。
4. 开启管理插件，但只允许运维网络访问管理台。
5. 配置磁盘空间、水位线、消息持久化和日志保留。
6. 确认应用主机可以访问 AMQP 端口 5672。

示例命令：

```bash
rabbitmq-diagnostics -q ping
rabbitmq-diagnostics -q check_running
```

### 7.2 应用配置要点

项目公共配置使用：

```properties
spring.rabbitmq.virtual-host=/
spring.rabbitmq.publisher-confirms=true
```

部署新环境时必须同时检查：

- RabbitMQ 主机和端口。
- virtual host、应用用户和密码。
- 消息前缀和交换机、队列命名。
- 是否需要 TLS。

### 7.3 消息验收

在 RabbitMQ 管理台确认：

- 应用用户连接成功。
- 交换机和队列已创建。
- 没有持续增长的未确认消息。
- 没有大量死信或重复消费。
- 测试环境消息没有进入其他环境的 virtual host。

## 8. Elasticsearch 部署

### 8.1 安装和基础配置

建议使用与现有测试或生产集群一致的 Elasticsearch 版本。不要只根据当前 Java 依赖推断 ES 版本，部署前应确认集群版本。

单机开发环境至少确认：

- HTTP 端口 9200 可访问。
- 数据目录已持久化。
- JVM heap 不超过主机内存的一半。
- 已设置索引生命周期或日志清理策略。
- 文件服务使用的账号拥有目标索引读写权限。

检查命令：

```bash
curl http://<ES_HOST>:9200/
curl http://<ES_HOST>:9200/_cluster/health
```

如果 ES 启用了认证和 HTTPS，应使用对应的认证参数，不要关闭安全配置来绕过问题。

### 8.2 文件访问日志验收

文件服务包含文件访问日志和 ES 相关模块。启动文件服务后检查：

1. 文件服务能连接 ES。
2. 文件上传成功后能产生访问记录。
3. 访问日志索引没有持续报错。
4. 索引保留策略不会无限增长。

## 9. Eureka 注册中心部署

Eureka 不是数据库等基础组件，但它是后端服务启动的必要平台服务。

### 9.1 启动

```powershell
cd "E:\Projects\0源代码202607 (2)\注册服务\tfcloud-eureka-server"
.\mvnw.cmd -DskipTests package
java -jar target\<eureka-jar>.jar
```

配置入口：

```text
注册服务/tfcloud-eureka-server/src/main/resources/application.properties
```

当前默认端口为 20000。

### 9.2 验收

打开：

```text
http://<EUREKA_HOST>:20000/
```

应用全部启动后，应看到至少以下服务注册：

- 业务服务
- 账号服务
- 文件服务

单机 Eureka 应保持不向自身注册、不拉取注册表；集群部署则需要重新规划 peer 节点和高可用策略。

## 10. 应用配置接入

基础设施准备好后，修改各应用环境配置中的以下内容：

```properties
eureka.client.serviceUrl.defaultZone=http://<EUREKA_HOST>:20000/eureka/

spring.datasource.url=jdbc:mysql://<MYSQL_HOST>:3306/<DATABASE>?useSSL=false&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
spring.datasource.username=<DB_USER>
spring.datasource.password=<DB_PASSWORD>

spring.redis.host=<REDIS_HOST>
spring.redis.port=6379
spring.redis.password=<REDIS_PASSWORD>
spring.redis.prefix=<ENV_PREFIX>

spring.rabbitmq.addresses=<MQ_HOST>:5672
spring.rabbitmq.username=<MQ_USER>
spring.rabbitmq.password=<MQ_PASSWORD>
spring.rabbitmq.virtual-host=<MQ_VHOST>
```

文件服务还需要：

```properties
spring.elasticsearch.uris=http://<ES_HOST>:9200
tofocus.file.baseUrl=https://<FILE_PUBLIC_HOST>/file/
tofocus.file.baseIntranetUrl=http://<FILE_INTERNAL_HOST>:21003/
tofocus.file.root=uploads
tofocus.file.backup=filebackup
```

实际配置文件中的占位符需要通过 Maven 资源过滤生成，不能把未过滤的 `@xxx@` 配置直接拿来运行。

## 11. 推荐启动顺序

```text
1. MySQL
2. Redis
3. RabbitMQ
4. Elasticsearch
5. Eureka
6. 账号服务
7. 文件服务
8. 业务服务
9. PC 管理端和微信小程序
```

每一步都应先完成健康检查，再启动下一层。出现连接失败时，不要同时启动所有服务，否则日志很难判断真实故障点。

## 12. 上线前验收清单

### 12.1 基础设施

- [ ] 所有服务使用专用账号，不使用 root 或默认管理账号。
- [ ] MySQL 三套数据库已创建并完成字符集、时区配置。
- [ ] MySQL 备份已验证可以恢复。
- [ ] Redis 已设置认证、内存和持久化策略。
- [ ] RabbitMQ virtual host、用户和权限已创建。
- [ ] RabbitMQ 没有跨环境共用消息空间。
- [ ] Elasticsearch 集群健康状态正常。
- [ ] Elasticsearch 已设置索引保留策略。
- [ ] 只开放必要端口，管理台不暴露公网。

### 12.2 应用接入

- [ ] 配置文件中的占位符已被 Maven 正确替换。
- [ ] Eureka 页面能看到业务、账号、文件服务。
- [ ] 三个应用使用同一套环境标识。
- [ ] OAuth2 地址和账号服务地址一致。
- [ ] 文件公网地址和内网地址均可访问。
- [ ] Flyway 迁移完成且没有 checksum 错误。
- [ ] 业务服务可以读写 Redis、RabbitMQ 和 MySQL。
- [ ] 文件服务可以读写文件库和 Elasticsearch。

### 12.3 业务验收

- [ ] PC 后台可以登录并加载菜单。
- [ ] 商城端可以完成微信登录、首页和商品查询。
- [ ] 商户端可以登录并查看订单。
- [ ] 骑手端可以登录并查看配送订单。
- [ ] 文件上传、图片访问正常。
- [ ] 下单、退款、消息、结算等关键链路完成测试。

## 13. 备份和恢复

### 13.1 MySQL

至少执行每日全量备份、重要环境 binlog 或增量备份、异机保存，并每月至少进行一次恢复演练。

示例：

```bash
mysqldump -h <MYSQL_HOST> -u <BACKUP_USER> -p \
  --single-transaction --routines --events \
  <DATABASE> > <DATABASE>-$(date +%F).sql
```

### 13.2 Redis

确认 RDB/AOF 文件位置、备份周期和恢复方法。登录态、购物车和缓存通常不应作为唯一数据源；恢复后需要按环境清理过期缓存。

### 13.3 RabbitMQ

备份用户、virtual host、交换机、队列、绑定和权限定义。业务消息是否需要备份，要根据消息幂等性和数据库最终状态决定。

### 13.4 Elasticsearch

生产环境使用快照仓库进行备份，不建议直接复制正在运行的数据目录。文件访问日志可重建时可采用较短保留周期；有审计要求时应单独制定保留策略。

## 14. 故障排查顺序

### 应用无法连接 MySQL

1. 检查 3306 网络连通性。
2. 检查 MySQL 用户允许的来源主机。
3. 检查数据库名、字符集和时区参数。
4. 检查应用 profile 是否连接了错误环境。
5. 检查数据库迁移是否被锁或 checksum 错误中断。

### 应用无法连接 Redis

1. 检查 6379 网络连通性。
2. 检查密码或 ACL 用户。
3. 检查 Redis 绑定地址和防火墙。
4. 检查 prefix 是否与其他环境冲突。

### 应用无法连接 RabbitMQ

1. 检查 5672，而不是只检查管理台 15672。
2. 检查 virtual host、用户权限和密码。
3. 检查交换机、队列和绑定是否成功创建。
4. 查看 RabbitMQ 日志和未确认消息数量。

### 文件服务无法使用 Elasticsearch

1. 访问 ES `/_cluster/health`。
2. 检查 `spring.elasticsearch.uris` 是否指向 HTTP 端口 9200。
3. 检查 ES 认证、TLS 和索引权限。
4. 检查磁盘空间和索引写入错误。

### 服务注册不到 Eureka

1. 检查 Eureka 的 20000 端口。
2. 检查 `defaultZone` 是否以 `/eureka/` 结尾。
3. 检查服务配置中的 hostname、端口和 profile。
4. 确认 Maven 资源过滤后没有残留 `@eureka...@` 占位符。

## 15. 安全要求

- 不要在文档、脚本、Git 或聊天记录中复制真实密码、支付密钥和 Token。
- 当前配置和 POM 中存在多套环境连接信息，部署前应统一改为密钥管理或环境变量，并轮换已经暴露的凭据。
- 生产环境禁止使用测试数据库、测试微信 AppID、测试支付回调和测试文件域名。
- Redis、RabbitMQ、Elasticsearch 管理接口只允许运维网络访问。
- 应用日志中不得打印数据库密码、Access Token、微信 session key 或支付签名原文。
- Flyway、数据库备份、文件备份和 Elasticsearch 快照都要记录版本和时间。
- 所有迁移、恢复和配置切换都应保留变更记录。

## 16. 后续应用启动

基础设施部署完成后，继续执行：[项目运行文档.md](./项目运行文档.md)，其中包含后端构建、服务启动、PC 管理端和三个微信小程序的运行步骤。

